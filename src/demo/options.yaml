title: Options
options:
  - type: string
    id: username
    name: Username
    defaultValue: "mrmikemccabe"

  - type: password
    id: password
    name: Password
    defaultValue: "12345"

  - type: boolean
    id: yes-or-no
    name: Yes or no?
    defaultValue: true

  - type: string
    id: cdn-username
    name: CDN Username
    defaultValue:
    placeholder: This is a placeholder

  - type: string
    id: cdn-key
    name: CDN Key
    defaultValue:

  - type: textarea
    id: textarea
    name: Textarea
    defaultValue:
    placeholder: This is a placeholder too
